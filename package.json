{
  "name": "gta6-neon-railway-ready",
  "version": "5.0.0",
  "private": true,
  "description": "Premium neon media library with persistent metadata, batch ingestion, cached thumbnails, and dedicated media pages.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  }
}
